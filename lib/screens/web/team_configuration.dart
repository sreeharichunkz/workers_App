import 'package:flutter/material.dart';

class TeamConfigurationScreen extends StatelessWidget {
  const TeamConfigurationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('🛠️ Team Configuration coming soon...'));
  }
}
